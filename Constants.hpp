#pragma once

const std::string filenameOfInputFile = "input.txt";                        // Ввести имя файла ввода
const std::string filenameOfOutputFile = "output.txt";                      // Ввести имя файла вывода

const int MAX = INT_MAX;
const int MIN = INT_MIN;